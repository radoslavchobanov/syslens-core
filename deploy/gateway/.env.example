# Replace with your locally built image or your own registry image and commit.
SYSLENS_GATEWAY_IMAGE=syslens-gateway:REPLACE_WITH_GIT_COMMIT
# Match the dedicated host directory owner. Never use UID 0.
GATEWAY_UID=1000
GATEWAY_GID=1000
